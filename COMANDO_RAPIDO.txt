# COMANDO RÁPIDO - Cole e execute este comando no PowerShell:

$url = Read-Host "`nCole a DATABASE_URL do Railway aqui"; if ($url) { $content = Get-Content .env -Raw; $content = $content -replace 'DATABASE_URL=.*', "DATABASE_URL=$url"; Set-Content .env -Value $content; Write-Host "`n✅ .env atualizado com sucesso!" -ForegroundColor Green; Write-Host "`n🔍 Testando conexão...`n" -ForegroundColor Cyan; node server/test-system.js; if ($LASTEXITCODE -eq 0) { Write-Host "`n🌱 Populando banco com dados de teste...`n" -ForegroundColor Cyan; npm run seed } }
